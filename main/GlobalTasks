1) Test all the project (move-constructor, == with oneself, explicit	(int != vector))

3) Logging

4) Write your own Double for truly comparation

5) Move semantics:
	- Copy-and-swap idiom
	- friend swap - why?
	- ADL and friend functions: if friend function defined in class then which kind of namespace this
	    function locate in?
almost but not fully undestanding...

5) Rewrite makefile
    - learn how write MakeFile to evoid re-compilition of files
    - may be it is necessary to add makefile at each package